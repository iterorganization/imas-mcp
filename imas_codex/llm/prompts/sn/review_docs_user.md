---
name: sn/review_docs_user
description: Single-item docs quality review for standard names generated by the generate_docs pipeline (Phase 8.1)
used_by: imas_codex.standard_names.workers.process_review_docs_batch
task: review
dynamic: true
schema_needs: []
---

Apply the rubric (provided in the system prompt) to the candidate below.

## Candidate

- **Standard name**: {{ item.id }}
- **Unit**: {{ item.unit | default('N/A', true) }}
- **Kind**: {{ item.kind | default('N/A', true) }}
- **Tags**: {{ item.tags | default([], true) | join(', ') }}
- **Description**: {{ item.description | default('(missing)', true) }}
- **Documentation**:

{{ item.documentation | default('(missing)', true) }}

Return JSON matching the schema defined in the system prompt; set
`source_id` and `standard_name` to `{{ item.id }}`.
