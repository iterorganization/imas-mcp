# Discovery Pattern Configuration
# DEPRECATED: This file has been split into modular components.
#
# New structure:
#   config/patterns/
#   ├── exclude.yaml                # Path exclusion patterns
#   ├── file_types.yaml             # File extension categories
#   └── scoring/
#       ├── base.yaml               # Dimension weights and thresholds
#       ├── data_systems.yaml       # IMAS, MDSplus, HDF5, UDA, etc.
#       └── physics.yaml            # Physics domain patterns
#
# Loader: imas_codex.config.discovery_config.DiscoveryConfig.load()
#
# This file is kept for backward compatibility reference only.
# The new modular configs are loaded by get_discovery_config().

version: "2.0"
description: "Discovery configuration - see split files for actual patterns"

# Redirect to new structure
config_files:
  exclude: "exclude.yaml"
  file_types: "file_types.yaml"
  scoring:
    base: "scoring/base.yaml"
    data_systems: "scoring/data_systems.yaml"
    physics: "scoring/physics.yaml"
