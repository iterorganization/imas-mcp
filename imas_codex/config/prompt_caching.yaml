# Providers requiring explicit cache_control breakpoints in messages.
# Implicit caching providers (OpenAI, DeepSeek, Grok) need no config.
# Source: https://openrouter.ai/docs/guides/best-practices/prompt-caching

providers:
  anthropic:
    match: [claude, anthropic]

  google:
    match: [gemini, google]
